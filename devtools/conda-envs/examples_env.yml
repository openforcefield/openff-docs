name: openff-docs-examples
channels:
  - conda-forge
dependencies:
  # Examples
  - openff-toolkit-examples =0.18
  - ambertools
  - openff-interchange =0.5
  - openff-nagl =0.5
  # openff-fragmenter
  # Runtime
  - pip
  - python=3.12
  - jupyterlab
  - ipywidgets
  # Cookbook
  - gitpython
  - nbconvert
  - nbformat
  - requests
  - packaging
  # Other deps
  - openff-qcsubmit
  - openmmforcefields =0.15
  - gromacs >=2021=nompi*
  - lammps
  - rich
  - jax
  - parmed >=4.3.1
  - nglview
  - psi4 =1.9.1  # probably want 1.10+
  - libint =2.9  # probably no longer needed
  # NAGL training dependencies - constraints are just to help solver
  # hopefully will be replaced by openff-nagl-training in future
  - pyarrow =17  # this can probably be bumped up to 20, which is still outdated
  - openff-recharge =0.5.3
  - dask-jobqueue
  - dgl =2.3.0
  # torchdata.datapipes was removed in 0.10.0
  - torchdata<=0.9.0
